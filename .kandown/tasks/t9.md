---
id: t9
title: Fix markdown heading hierarchy
status: Done
order: 3
created: 2026-05-04
priority: P1
assignee: codex
ownerType: ai
tools: filesystem, cli, chrome-devtools
report: |
  ## Changes
  - Fixed Markdown prose heading hierarchy in `src/styles/global.css`.
  - Added explicit styles for `h1` through `h6`, including fluid sizes, line heights, section spacing, and distinct lower-level heading treatments.
  - Saved Chrome DevTools screenshots before and after the fix:
    - `.kandown/markdown-headings-before.png`
    - `.kandown/markdown-headings-after.png`

  ## DevTools Measurements
  - Before: `h2` and `h3` rendered at 16px; `h4`, `h5`, and `h6` inherited paragraph-like 16px styling.
  - After: `h2` 42.64px, `h3` 31.15px, `h4` 24.17px, `h5` 16.8px, `h6` 13.12px.

  ## Verification
  - `PATH="/opt/homebrew/bin:$PATH" ./node_modules/.bin/astro check` passes with 0 errors, 0 warnings, and 0 hints.
  - `./node_modules/.bin/tsc --noEmit --pretty false` passes.
  - `PATH="/opt/homebrew/bin:$PATH" ./node_modules/.bin/astro build` passes and builds 22 pages.
---

# Fix markdown heading hierarchy

## Context

Markdown headings in rendered blog posts do not have a clear typographic hierarchy. Use Chrome DevTools screenshots and computed styles on the Markdown kitchen-sink post, then fix prose heading styles.

## Subtasks

- [x] Read project instructions, Kandown rules, and current prose CSS.
  report: Read `AGENTS.md`, `README.md`, `.kandown/AGENT_KANDOWN.md`, and `src/styles/global.css`.
- [x] Inspect heading rendering in Chrome DevTools.
  report: DevTools computed styles showed prose `h2` and `h3` at 16px, while `h4`, `h5`, and `h6` inherited normal paragraph sizing. Saved the before screenshot to `.kandown/markdown-headings-before.png`.
- [x] Fix prose heading sizes and spacing.
  report: Added explicit `.prose-content` styles for `h1` through `h6`, including a clear size scale, border-separated `h2` sections, tuned line heights, and distinct `h5`/`h6` treatment.
- [x] Verify with checks, build, and browser screenshot.
  report: Re-measured heading computed styles in Chrome DevTools, saved `.kandown/markdown-headings-after.png`, then ran Astro check, TypeScript no-emit, and Astro build successfully.

## Notes

Target page: `/blog/markdown-kitchen-sink/`.
