---
id: t8
title: Fix theme contrast and add markdown fixture posts
status: Done
order: 2
created: 2026-05-04
priority: P1
assignee: codex
ownerType: ai
tools: filesystem, cli
report: |
  ## Changes
  - Strengthened selected theme toggle contrast with dedicated CSS for `[data-theme-toggle-button][aria-pressed="true"]`.
  - Expanded prose styles for lists, blockquotes, task-like content, tables, images, details, and MDX fixture components.
  - Added `src/content/posts/markdown-kitchen-sink.md`.
  - Added `src/content/posts/mdx-kitchen-sink.mdx`.
  - Added `src/components/content/MdxCallout.astro` and `src/components/content/MdxMetric.astro` for imported MDX component coverage.
  - Updated `CHANGELOG.md`.

  ## Decisions
  - Kept the fixtures published so they appear in `/blog`, tag archives, RSS, and static route generation.
  - Used imported Astro components in MDX because locally declared JSX components did not survive the Astro MDX production render.

  ## Verification
  - `PATH="/opt/homebrew/bin:$PATH" ./node_modules/.bin/astro check` passes with 0 errors, 0 warnings, and 0 hints.
  - `./node_modules/.bin/tsc --noEmit --pretty false` passes.
  - `PATH="/opt/homebrew/bin:$PATH" ./node_modules/.bin/astro build` passes and builds 22 pages.
  - Local HTTP checks return 200 for `/blog/markdown-kitchen-sink/` and `/blog/mdx-kitchen-sink/`.
  - Selected button contrast is 18.01:1 in light mode and 15.81:1 in dark mode.
---

# Fix theme contrast and add markdown fixture posts

## Context

The theme selector needs verified readable contrast in light and dark modes. Add one Markdown post and one MDX post with broad markup coverage so the blog pipeline can be tested against common Markdown and MDX features.

## Subtasks

- [x] Read project instructions, Kandown rules, content schema, and existing posts.
  report: Read `AGENTS.md`, `README.md`, `.kandown/AGENT_KANDOWN.md`, `src/content/config.ts`, and current post frontmatter examples.
- [x] Fix selected theme button contrast.
  report: Added dedicated `[data-theme-toggle-button][aria-pressed="true"]` CSS so selected buttons always use `--primary` and `--primary-foreground`, independent from Tailwind variant generation. Also expanded prose CSS for common Markdown fixtures.
- [x] Add Markdown coverage post.
  report: Added `src/content/posts/markdown-kitchen-sink.md` with headings, emphasis, lists, task lists, quotes, code fences, a table, image, raw HTML, details, and a horizontal rule.
- [x] Add MDX coverage post.
  report: Added `src/content/posts/mdx-kitchen-sink.mdx` with frontmatter, imports, exports, imported Astro components, expressions, mapped JSX, a JSX table, and details. Moved fixture component styles to `global.css` because raw CSS braces inside MDX `<style>` are parsed as MDX expressions during build.
- [x] Run checks/build and update completion report.
  report: Ran Astro check, TypeScript no-emit, Astro build, local HTTP checks for both new posts, and a token-based contrast calculation for the selected theme button.

## Notes

Keep the posts published so they show up in the blog index and generated routes.
